# Optional OpenAI-compatible endpoint for local development only.
VITE_MODEL_BASE_URL=https://api.openai.com/v1
VITE_MODEL_NAME=
# Never commit real API keys. Store secrets in the OS credential vault.

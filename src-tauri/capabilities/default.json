{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal desktop permissions for the initial shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
